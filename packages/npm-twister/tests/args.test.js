const lib = require('../src/args')

describe('npm-twister', function () {
    describe('args', function () {
        it('test', function () {
            expect(0).toEqual(0)
        })
    })
})
